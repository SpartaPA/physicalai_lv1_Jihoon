# 모듈 ④ 발표 자료 — 픽앤플레이스 자세 추정과 궤적 생성

> 이 파일을 `presentation.md` 로 복사해 채우세요. 다섯 절은 모두 필수입니다.
> 문제 6 의 검증 셀이 `presentation.md` 의 존재와 다섯 절 제목을 확인합니다.
> 이름: `___` / 제출일: `___`

---

## 1. 파이프라인 전체 구조

- 좌표계 체인 (base -> link -> camera -> object) 그림 또는 다이어그램: `___`
- 각 노트북·모듈이 맡는 역할 (01 파이프라인 / 02 보간 / 03 자세 추정·시연): `___`
- 데이터 흐름: 카메라 점군 -> `PosePipeline` -> base 점군 -> PCA/Kabsch -> 목표 자세 -> SLERP + 스플라인 -> 애니메이션
- 모듈 ③ 에서 가져온 함수와 이번에 새로 만든 함수 구분: `___`

## 2. 자세 추정 결과와 오차

- PCA 주축 3개와 고유값: `___`
- Kabsch 로 추정한 회전과 참값 사이 각도 오차: `___` 도, 병진 오차: `___`
- 이상치 제거 전후 오차: `___` -> `___`
- 정합 전후 그림 (03 노트북 캡처): `___`

## 3. 노이즈·점 개수가 정확도에 미친 영향

- 노이즈 0 ~ 0.25 스윕에서 오차가 어떻게 커졌는가: `___`
- 점 개수 30 / 90 / 270 계열 비교 — 점이 많을수록 오차가 줄어드는 정도 (대략 몇 배): `___`
- 구형 점군에서 PCA 주축이 불안정해진 이유 (고유값 관점): `___`
- 오차 그래프 (03 노트북 캡처): `___`

## 4. 현재 구현의 한계

- 자세 추정: `___` (예: 대응이 알려진 경우에만 Kabsch 가 동작, 대칭 물체에서 PCA 축 부호 모호 등)
- 궤적 생성: `___` (예: 충돌·관절 한계 미고려, 속도·가속도 한계 미반영 등)
- 파이프라인: `___` (예: 관절 1개 단순화, 카메라 캘리브레이션 오차 무시 등)
- 시연: `___`

## 5. 개선한다면 무엇을 어떻게

- `___` (무엇을) -> `___` (어떻게) — 예: ICP 로 대응 없는 정합, RANSAC 평면 피팅, 최소 저크 궤적
- `___`
- `___`
- 개선 효과를 어떻게 측정할 것인지: `___`

---

### 부록 — 재현 정보

- Python / 주요 패키지 버전 (`requirements.txt`): `___`
- 난수 시드: `np.random.default_rng(42)`
- 세 노트북 Restart Kernel and Run All 통과 여부: `___`
- `demo.gif` 프레임 수: `___`
